/target
*.img
test_files*
tmp
/docs.worktrees/

# Authentic kernel dirty-journal fixtures are generated by
# scripts/make-dirty-fixture.sh (privileged) and are large binaries — never
# commit them. The directory's .gitkeep + README.md stay tracked.
tests/fixtures/dirty/*.img
